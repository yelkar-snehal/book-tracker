'use client'; // Mark this as a Client Component since we're importing Client Components

import BooksContainer from '@/app/components/BookConatiner';

export default function Home() {
  return <BooksContainer />;
}
